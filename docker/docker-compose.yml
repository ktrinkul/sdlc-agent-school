version: "3.8"

services:
    agent:
        build:
            context: ..
            dockerfile: docker/Dockerfile
        env_file:
            - ../.env
        volumes:
            - ../src:/app/src
            - ../prompts:/app/prompts
            - agent-data:/app/data
        command: ["--help"]

volumes:
    agent-data:
